using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/* TODO:
    STEP 1:
        We will make pressing specific buttons do specific things to an attatched shipcontroller, assuming the ship has one
    STEP 2:
        Add any extra features as nessicary
*/

public class InputManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
